apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "loan-service.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "loan-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "loan-service.selectorLabels" . | nindent 6 }}
  # Ensure zero-downtime rollouts: one new pod starts before an old one is removed.
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        {{- include "loan-service.selectorLabels" . | nindent 8 }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      # ── Security ────────────────────────────────────────────
      securityContext:
        runAsNonRoot: {{ .Values.securityContext.runAsNonRoot }}
        runAsUser: {{ .Values.securityContext.runAsUser }}

      # ── Graceful shutdown ────────────────────────────────────
      # Allow 30 s for in-flight requests to drain before SIGKILL.
      terminationGracePeriodSeconds: 30

      # ── Init Containers ──────────────────────────────────────
      # Mirrors the banking-app k8s manifest: waits for MySQL,
      # RabbitMQ, and account-service before the main container starts.
      initContainers:
        {{- if .Values.initContainers.waitForMySQL.enabled }}
        - name: wait-for-mysql
          image: {{ .Values.initContainers.waitForMySQL.image }}
          command: ['sh', '-c', 'until nc -z {{ .Values.initContainers.waitForMySQL.host }} {{ .Values.initContainers.waitForMySQL.port }}; do echo "Waiting for MySQL..."; sleep 3; done']
        {{- end }}
        {{- if .Values.initContainers.waitForRabbitmq.enabled }}
        - name: wait-for-rabbitmq
          image: {{ .Values.initContainers.waitForRabbitmq.image }}
          command: ['sh', '-c', 'until nc -z {{ .Values.initContainers.waitForRabbitmq.host }} {{ .Values.initContainers.waitForRabbitmq.port }}; do echo "Waiting for RabbitMQ..."; sleep 3; done']
        {{- end }}
        {{- if .Values.initContainers.waitForAccount.enabled }}
        - name: wait-for-account
          image: {{ .Values.initContainers.waitForAccount.image }}
          command: ['sh', '-c', 'until wget -qO- {{ .Values.initContainers.waitForAccount.healthUrl }} 2>/dev/null | grep -q UP; do echo "Waiting for account-service..."; sleep 5; done']
        {{- end }}

      containers:
        - name: loan-service
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}

          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort }}
              protocol: TCP

          # ── Environment ───────────────────────────────────────
          envFrom:
            - secretRef:
                name: {{ include "loan-service.fullname" . }}-secret
            - configMapRef:
                name: {{ include "loan-service.fullname" . }}-config

          # ── Probes ────────────────────────────────────────────
          # startupProbe: gates liveness/readiness until the app has fully
          # booted and connected to MySQL + RabbitMQ (exponential backoff can take ~60 s).
          startupProbe:
            httpGet:
              path: {{ .Values.probes.startup.path }}
              port: http
            initialDelaySeconds: {{ .Values.probes.startup.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.startup.periodSeconds }}
            failureThreshold: {{ .Values.probes.startup.failureThreshold }}

          # readinessProbe: removes pod from Service endpoints if not ready.
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readiness.path }}
              port: http
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
            failureThreshold: {{ .Values.probes.readiness.failureThreshold }}

          # livenessProbe: restarts the pod if it becomes permanently unhealthy.
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.liveness.path }}
              port: http
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
            failureThreshold: {{ .Values.probes.liveness.failureThreshold }}

          # ── Resources ─────────────────────────────────────────
          resources:
            {{- toYaml .Values.resources | nindent 12 }}

          # ── Container Security ────────────────────────────────
          securityContext:
            allowPrivilegeEscalation: {{ .Values.securityContext.allowPrivilegeEscalation }}
            readOnlyRootFilesystem: {{ .Values.securityContext.readOnlyRootFilesystem }}
            capabilities:
              {{- toYaml .Values.securityContext.capabilities | nindent 14 }}
